services:
  discord-bot:
    build: .
    pull_policy: always
    container_name: bogabot
    restart: unless-stopped
    env_file:
      - stack.env
    volumes:
      - data:/data

volumes:
  data: