services:
  discord-bot:
    build: .
    pull-policy: always
    container_name: bogabot
    restart: unless-stopped
    env_file:
      - stack.env
    volumes:
      - data:/data

volumes:
  data: